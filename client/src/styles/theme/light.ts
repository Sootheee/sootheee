import { palette } from '../common';

const lightTheme = {
  background: palette.beige100,

  button: {
    background: palette.softBlack100,
    disabled: palette.softBlack40,
    color: palette.white100,
  },
};

export { lightTheme };
